using TMPro;
using UnityEngine;

public class DisplayTextFade : MonoBehaviour
{
    public TMP_Text _Text;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
